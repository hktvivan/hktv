# Rename this file to .cursorrules and place to your working folder to effective

# Planning & Execution Protocol (Human Drive Pattern)
- **Always Plan First**: For any task more complex than a typo fix, you must enter "Plan Mode" first.
- **The Planning Template**: Every plan must include:
  1. **Analysis**: Current state of the relevant files.
  2. **Proposed Changes**: Specific logic changes per file.
  3. **Verification**: How you will test the change (specific commands).
- **Wait for Approval**: Do not execute shell commands or write files until I explicitly say "Proceed" or "Approved."
- **Cursor-Style Reasoning**: Think step-by-step about side effects and potential breaking changes before proposing.

# Important Notes
- A secret named **"AI-AccessTokens-DB"** is stored in the **Microsoft Secret Store**, Vault **LocalStore**. It provides access to the KeePass database **"AI_Access_Tokens.kdbx"** located in the **Desktop/記事** folder on this PC.
- **Do NOT echo the secret directly or with `-Verbose` in the Claude session.** Always mask or suppress the secret value when retrieving it.
- **Credential Handling Rule**: When working with credentials (username, password, password phrases, tokens, API keys, TOTP secrets), agents must ONLY use one of these two approaches:
  - **Broker**: Retrieve credentials through a credential broker or secret manager (e.g., Microsoft Secret Store, Keychain, Vault) — never hardcode or inline them.
  - **Proxy**: Pass credentials to commands exclusively via pipe (`|`) — e.g., `Get-Secret -Name "X" | some-command`. Never pass credentials as command-line arguments, environment variables, or inline in scripts.
  - **Never** embed credentials in source code, config files, scripts, command-line arguments, or chat output.
  - **Debug Output Rule**: When running scripts for debugging or verification, NEVER print credential values (passwords, phrases, secrets, tokens) to stdout — even partially, even for "just checking." Use length-only output (e.g., `len(password)`) or masked output (e.g., `password[:3]***`). Stdout from scripts appears directly in the chat session and is logged. This applies to ALL credential components: base passwords, TOTP secrets, concatenated passwords, API keys, etc.

# Unit Testing Rule
- **Every code change requires unit tests.** This applies to all code — including standalone Python scripts, utilities, and helper functions.
- **Before marking a task complete**, write or update tests that cover the changed logic, and run them to confirm they pass.
- **Test edge cases**: invalid input, missing fields, boundary values — not just the happy path.
- **For scripts without an existing test suite**, add at least one runnable self-check or `pytest` test alongside the code.

# Mandatory Skills Protocol
5 skills are configured. You MUST follow these trigger rules on EVERY response:

1. **simplify** (DEFAULT ON, toggle per session): After ANY code change (Write, Edit, or Bash that modifies files), you MUST invoke the simplify skill. Report what it found. Do NOT wait for the user to ask. Say "simplify on" or "simplify off" to toggle.

2. **security-review** (DEFAULT ON): Before ending any response that modified code or touched security-sensitive areas, you MUST invoke the security-review skill. Report findings inline.

3. **Superpowers** (DEFAULT OFF, toggle per session): When enabled, follow Superpowers methodology (brainstorming → planning → subagent execution → TDD → review). Takes priority over default workflow.

# Skill Reporting Rules
- Every skill invocation MUST produce visible findings in the response.
- Do NOT silently skip skills. If a trigger condition is met, the skill MUST run.
- When multiple skills fire, include a "## Skill Reports" section summarizing findings.